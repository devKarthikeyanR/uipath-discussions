<!-- PROJECT-CONTEXT:START -->
<!-- discovery-metadata: cs=1 xaml=16 deps=4 -->
# ApplicationHealthCheckAfterPatch — Project Context

> Auto-generated by project discovery agent. Regenerate after significant project changes.

## Overview

| Property | Value |
|----------|-------|
| **Name** | ApplicationHealthCheckAfterPatch |
| **Type** | Process (REFramework — Transactional Business Process, State Machine) |
| **Description** | "C# Version" (per project.json) |
| **Target Framework** | Windows |
| **Expression Language** | CSharp |

## Dependencies

| Package | Version | Category | Description |
|---------|---------|----------|--------------|
| UiPath.Excel.Activities | 3.6.1 | Excel | Reads Config.xlsx (Settings/Constants/Assets) |
| UiPath.System.Activities | 26.6.2 | Core/System | Logging, exceptions, process control |
| UiPath.Testing.Activities | 25.10.2 | Testing | Powers the 6 Test Case workflows in `Tests/` |
| UiPath.UIAutomation.Activities | 26.10.1 | UI Automation | Application interaction (target: Outlook, per `Applications/Outlook`) |

## Project Structure

```
ApplicationHealthCheckAfterPatch/
├── Main.xaml                        # Entry point — REFramework state machine (Init/GetTxn/Process/End)
├── Framework/                       # 9 REFramework core workflows
│   ├── InitAllSettings.xaml         (406 lines — authored: reads Config.xlsx + assets)
│   ├── GetTransactionData.xaml      (245 lines — authored)
│   ├── SetTransactionStatus.xaml    (986 lines — authored)
│   ├── RetryCurrentTransaction.xaml (318 lines — authored)
│   ├── TakeScreenshot.xaml          (206 lines — authored)
│   ├── InitAllApplications.xaml     (68 lines — STUB: only a log message, no app logic)
│   ├── CloseAllApplications.xaml    (63 lines — STUB)
│   ├── KillAllProcesses.xaml        (62 lines — near-stub, standard REFramework)
│   └── Process.xaml                 (74 lines — STUB: log message + placeholder Comment only)
├── Tests/                           # 6 Test Case xaml + Tests.xlsx (UiPath.Testing.Activities)
├── Config/                          # BuildConfigWorkbook.ps1, Config.xlsx
├── Data/                            # Config.xlsx + History/Input/Output/Temp (all placeholder.txt only)
├── Applications/Outlook/            # EMPTY — planned Outlook app-automation library, not yet authored
├── Reporting/                       # EMPTY — planned
├── Templates/                       # EMPTY — planned (likely HTML report template)
├── Documentation/                   # EMPTY
├── .local/.codedworkflows/ObjectRepository.cs  # Coded Object Repository — no descriptors defined yet
├── entry-points.json, project.json, project.uiproj
└── README.md, LICENSE
```

| File Type | Count |
|-----------|-------|
| .xaml (authored, excl. dot-dirs/obj/bin) | 16 |
| .cs | 1 (empty ObjectRepository descriptor shell) |

## Entry Points

| File | Input Arguments | Output Arguments | Purpose |
|------|----------------|------------------|---------|
| Main.xaml | `in_OrchestratorQueueName` (string, optional), `in_OrchestratorQueueFolder` (string, optional) | none | REFramework top-level state machine: Initialization → Get Transaction Data → Process Transaction → End Process |

## Conventions

- **Namespace**: none custom; default REFramework namespaces (`UiPath.Core`, `UiPath.Core.Activities`, System.*)
- **File naming**: PascalCase workflow files under `Framework/`; `*TestCase.xaml` under `Tests/`
- **Base class**: Standard UiPath REFramework (State Machine variant of Transactional Business Process template)
- **Error handling**: TryCatch around Initialization state with `SystemException` variable; `SetTransactionStatus.xaml` distinguishes Success / BusinessRuleException / SystemException; `MaxConsecutiveSystemExceptions` / `ShouldMarkJobAsFaulted` config-driven kill switch
- **Argument prefixes**: `in_` for InArguments, `out_` for OutArguments (e.g. `in_Config`, `out_Config`, `in_TransactionItem`)
- **Config source**: `Data\Config.xlsx`, sheets `Settings`, `Constants`, `Assets` (loaded via `InitAllSettings.xaml`, merged with Orchestrator assets — assets override sheet values)

## Key Workflows

| Workflow | Purpose | Services/Activities Used |
|----------|---------|--------------------------|
| Main.xaml | Orchestrates the whole run via State Machine | InvokeWorkflowFile (all Framework/* workflows), LogMessage, Assign |
| Framework/InitAllSettings.xaml | Loads Config.xlsx (Settings/Constants) + Orchestrator assets into `Config` dictionary | Excel, Orchestrator Asset activities |
| Framework/GetTransactionData.xaml | Fetches next transaction (queue-based by default) | Orchestrator Queue activities |
| Framework/Process.xaml | **Not yet implemented** — placeholder Comment "// Invoke steps of the process" | — |
| Framework/SetTransactionStatus.xaml | Reports Success/BRE/SE back to the queue | Orchestrator Queue activities |
| Framework/InitAllApplications.xaml | **Not yet implemented** — only logs "Opening applications..." | — |
| Framework/CloseAllApplications.xaml | **Not yet implemented** — stub | — |
| Framework/TakeScreenshot.xaml | Captures screenshot on system exception | UI Automation |
| Framework/RetryCurrentTransaction.xaml | Retry logic for local (non-queue) transaction sources | — |
| Framework/KillAllProcesses.xaml | Kills configured process names at start/end | System |
| Tests/*.xaml (6 files) | UiPath Testing Activities test cases mirroring each Framework workflow + Main | UiPath.Testing.Activities |

## Shared Resources

- `Data\Config.xlsx` — Settings/Constants/Assets sheets (standard REFramework keys only: MaxRetryNumber, OrchestratorQueueName, OrchestratorQueueFolder, MaxConsecutiveSystemExceptions, ShouldMarkJobAsFaulted, logging message templates, etc. — no Outlook/app-specific keys added yet)
- `Config\BuildConfigWorkbook.ps1` — PowerShell script that generates/rebuilds `Config.xlsx`
- `.local\.codedworkflows\ObjectRepository.cs` — Coded Object Repository shell, no screens/elements defined
- `Data\History`, `Data\Input`, `Data\Output`, `Data\Temp` — placeholder folders (only `placeholder.txt` present)

## Architecture

- **Pattern**: UiPath REFramework — State Machine variant of the Transactional Business Process template
- **Data flow**: Config.xlsx + Orchestrator assets → `Config` dictionary → passed into every Framework workflow; transaction data flows from `GetTransactionData` → `Process` → `SetTransactionStatus`
- **Execution flow**: Init (settings + apps) → loop { Get Transaction → Process → Set Status } → End (close apps, kill processes)
- **External integrations**: Orchestrator queues (default transaction source), Orchestrator assets/credentials, planned Outlook UI automation (`Applications/Outlook` folder exists but is empty)
- **Current build state**: This is a REFramework scaffold with the orchestration skeleton (Main.xaml, GetTransactionData, SetTransactionStatus, InitAllSettings, RetryCurrentTransaction, TakeScreenshot) fully wired, but the business-specific pieces — `Process.xaml`, `InitAllApplications.xaml`, `CloseAllApplications.xaml`, the `Applications/Outlook` library, `Reporting/`, and `Templates/` — are still empty or stub placeholders awaiting authored logic.

## Code Patterns

- All expression code is C# (`CSharpValue`/`CSharpReference`), not VB
- LogMessage activities used liberally with Info/Trace levels and descriptive DisplayNames
- InvokeWorkflowFile used throughout Main.xaml to call each Framework/* workflow with explicit argument dictionaries

## Quick Reference

- **Run**: `uip rpa run-file --file-path "Main.xaml" --project-dir "C:\KARKA-SAAS-DEV\uipath-discussions\ApplicationHealthCheckAfterPatch"`
- **Validate**: `uip rpa get-errors --project-dir "C:\KARKA-SAAS-DEV\uipath-discussions\ApplicationHealthCheckAfterPatch"`
- **Key files to read first**: `Main.xaml`, `Framework/Process.xaml`, `Data/Config.xlsx`
<!-- PROJECT-CONTEXT:END -->
